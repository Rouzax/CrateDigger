# Dev tooling (pinned). CI and pre-commit install from this file.
# This file is the authoritative set of pinned dev-tool versions; the
# [project.optional-dependencies].dev extra in pyproject.toml overlaps but defers to this.
ruff==0.15.18
pyright==1.1.410
vulture==2.16
pre-commit==4.6.0
pytest==9.1.1
pytest-mock==3.15.1
pytest-cov==7.1.0
