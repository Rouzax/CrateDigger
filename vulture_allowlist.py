"""Vulture allowlist: names that are used dynamically or via framework hooks
and would otherwise be reported as dead code. Each entry needs a comment.

Populated in the vulture task; intentionally minimal to start.
"""
